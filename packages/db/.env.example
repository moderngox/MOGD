# Local dev: a file-based SQLite DB, independent of any SPIDRA database.
DATABASE_URL=file:./local.db

# Production: a Turso (libSQL) database created for MOGD specifically.
# DATABASE_URL=libsql://<your-mogd-db>.turso.io
# DATABASE_AUTH_TOKEN=<turso-auth-token>
