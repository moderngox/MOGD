/**
 * physique — goal compilation, priority muscles and canonical muscle model
 * domain boundary. The raw physique-priority selection (this file's export)
 * is captured starting M1; compiling it into canonical muscle priorities
 * and a training bias is M3 scope (docs/ARCHITECTURE.md §5-6).
 */
export { PHYSIQUE_PRIORITY_OPTIONS, MAX_PHYSIQUE_PRIORITIES, type PhysiquePriority } from "./priorities";
