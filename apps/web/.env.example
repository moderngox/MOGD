# Auth.js. Generate with: npx auth secret
AUTH_SECRET=

# See packages/db/.env.example and packages/media/.env.example — apps/web
# reads the same DATABASE_URL/DATABASE_AUTH_TOKEN and R2_* vars at runtime.
DATABASE_URL=file:../../packages/db/local.db
